from django.apps import AppConfig

class DoctoraddbyadminConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'DoctorAddByAdmin'
    verbose_name = "Doctors (Admin)"

